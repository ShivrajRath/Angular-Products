var planModule = angular.module('cart', []);

planModule.controller('CartIconController', ['$scope', '$rootScope', 'phoneModel',
    function($scope, $rootScope, phoneModel) {
        // Check how to remove this controller and instead use 
    }
]);
